server:
  port: 80
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tender
    username: postgres
    password: cyberhack25

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

  servlet:
    multipart:
      enabled: true
      max-request-size: 10000MB
      max-file-size: 10000MB

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  file:
    name: projects.log

app:
  ip-address: "192.168.222.171"
  admin:
    username: test
    password: root123

file:


